image: alpine:latest

pages:
  stage: deploy
  tags:
    - gitlab-runner
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r ai-assets public/
    - cp -r mcps public/
  artifacts:
    paths:
      - public
  only:
    - main
    - working
